# VisionGate data layer (Role C)
# Core — needed to run the API + dashboard on any machine:
Flask>=3.0
requests>=2.31
Werkzeug>=3.0        # password hashing for the admin login (ships with Flask)

# Production WSGI server (python serve.py):
waitress>=3.0

# Tests:
pytest>=8.0

# Real face recognition — only needed when RECOGNIZER_BACKEND = "face_recognition"
# (typically on the Raspberry Pi). Left commented because dlib is heavy to
# build on Windows; the stub backend needs none of this.
# face_recognition>=1.3
# numpy>=1.24
